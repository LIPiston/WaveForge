# 🧪 WaveForge 壁纸同步功能完整测试指南

## ✅ 测试结果总结

### 主进程壁纸检测 - **已验证通过** ✅

运行测试脚本验证：
```bash
node test-wallpaper.mjs
```

**测试结果**：
- ✅ 系统支持检查通过
- ✅ PowerShell 可用
- ✅ 成功读取注册表壁纸路径（包含中文路径）
- ✅ 壁纸文件存在
- ✅ URL 转换正确

**已修复的问题**：
- 原代码无法正确处理包含中文字符的壁纸路径
- 已更新为使用 `chcp 65001` 切换到 UTF-8 代码页
- 文件：`desktop/main.js:55-78`

---

## 🚀 如何启动和测试应用

### 第一步：启动 Vite 开发服务器

打开第一个终端窗口：

```bash
cd D:\opencode\waveforge
npm run dev
```

**预期输出**：
```
VITE v6.x.x  ready in xxx ms

➜  Local:   http://127.0.0.1:3000/
➜  Network: http://0.0.0.0:3000/
```

✅ 确保看到 "ready" 消息

---

### 第二步：启动 Electron 应用

打开第二个终端窗口：

```bash
cd D:\opencode\waveforge
npm run dev:electron
```

**预期输出**：
```
🚀 启动 Electron...
```

✅ 应该会弹出 Electron 窗口，显示 WaveForge 应用界面

---

### 第三步：进入桌面模式

在 WaveForge 应用中：

1. 如果当前是最小化播放器视图（immersive mode），找到切换按钮
2. 点击切换到**桌面模式**（Desktop Mode）
3. 应该看到 3D 歌单轮播界面

✅ 确认已进入桌面模式

---

### 第四步：打开设置面板

1. 点击右上角的**齿轮图标** ⚙️
2. 设置面板应该从右侧滑出
3. 向下滚动找到 **"Wallpaper Engine 同步"** 选项

✅ 确认设置面板已打开

---

### 第五步：启用壁纸同步

1. 找到 **"Wallpaper Engine 同步"** 开关
2. 点击开关将其切换到 **ON** 状态
3. 开关应该变为绿色/激活状态
4. 此时应该立即看到应用背景变为当前系统壁纸

✅ 壁纸同步已启用

**重要提示**：
- 设置会自动保存到 `localStorage`
- 下次启动应用时会保持开启状态

---

### 第六步：测试自动检测

#### 测试 6.1：更改系统壁纸

1. 打开 Windows 设置 → 个性化 → 背景
2. 选择一张新的壁纸图片
3. 点击应用

**预期结果**：
- ⏱️ 等待最多 **10 秒**
- 🖼️ WaveForge 应用背景应该自动更新为新壁纸
- ✨ 有平滑的淡入淡出动画效果（0.6 秒过渡）

**如何验证**：
- 在第二个终端（运行 Electron 的窗口）查看日志
- 应该看到：`🖼️ 系统壁纸已变化: file:///C:/...`

---

#### 测试 6.2：连续切换壁纸

1. 再次更改 Windows 壁纸
2. 等待 10 秒
3. 重复 3-5 次

**预期结果**：
- ✅ 每次都能正确检测并更新
- ✅ 动画流畅，无卡顿
- ✅ 不会出现内存泄漏

---

#### 测试 6.3：关闭壁纸同步

1. 在设置面板中将 **"Wallpaper Engine 同步"** 开关切换到 **OFF**
2. 更改 Windows 壁纸
3. 等待 10 秒

**预期结果**：
- ❌ 应用背景不再响应系统壁纸变化
- 🌊 应用显示默认的**液态玻璃背景**（LiquidGlassBackground）
- 5 个彩色渐变气泡动画

---

## 🐛 调试方法

### 查看日志

#### 主进程日志（Electron）

在运行 `npm run dev:electron` 的终端窗口中查看。

**关键日志**：
```
成功获取壁纸路径: C:\Users\...\wallpaper.jpg
获取壁纸路径失败: [错误信息]
壁纸监听出错: [错误信息]
```

#### 渲染进程日志（前端）

1. 在 Electron 窗口中按 **F12** 或 **Ctrl+Shift+I**
2. 打开 Chrome 开发者工具
3. 切换到 **Console** 标签

**关键日志**：
```javascript
🖼️ 系统壁纸已变化: file:///C:/Users/.../wallpaper.jpg
```

---

### 手动测试命令

#### 测试注册表读取

在 PowerShell 中运行：

```powershell
chcp 65001
Get-ItemPropertyValue -Path 'HKCU:\Control Panel\Desktop' -Name Wallpaper
```

**预期输出**：当前壁纸的完整路径，包括中文字符应该正确显示。

---

#### 测试文件存在性

```powershell
$path = Get-ItemPropertyValue -Path 'HKCU:\Control Panel\Desktop' -Name Wallpaper
Test-Path $path
```

**预期输出**：`True`

---

### 检查 localStorage

在开发者工具的 Console 中运行：

```javascript
// 检查壁纸同步开关状态
localStorage.getItem('wallpaperSyncEnabled')
// 应该返回 "true" 或 "false"

// 检查壁纸管理器设置
localStorage.getItem('desktopWallpaperSettings')
// 应该返回 JSON 字符串
```

---

## ⚠️ 已知限制

### 1. 检测延迟
- **10 秒轮询间隔**：壁纸变化后最多需要等待 10 秒
- 这是设计行为，避免频繁查询注册表影响性能

### 2. Wallpaper Engine 支持
- **动态壁纸显示为静态截图**：无法播放视频壁纸
- 完整的动态壁纸支持计划在未来版本实现

### 3. 平台限制
- **仅支持 Windows 10/11**
- macOS 和 Linux 暂不支持

### 4. 文件路径限制
- 壁纸路径必须是本地文件
- 不支持网络路径（UNC 路径）

---

## 🔧 常见问题排查

### 问题 1: 壁纸不自动更新

**症状**：更改系统壁纸后，应用背景没有变化

**排查步骤**：

1. **检查开关状态**
   - 确认 "Wallpaper Engine 同步" 开关已开启（绿色）

2. **检查日志**
   - 打开开发者工具（F12）查看是否有错误
   - 查看 Electron 终端是否有错误输出

3. **检查权限**
   - 确保应用有权限读取注册表
   - 尝试以管理员身份运行

4. **手动测试**
   - 运行 `node test-wallpaper.mjs` 验证检测逻辑

5. **重启应用**
   - 关闭 Electron 窗口
   - 重新运行 `npm run dev:electron`

---

### 问题 2: 图片显示异常

**症状**：背景一片空白或图片拉伸变形

**可能原因**：
- 图片路径转换错误
- 图片格式不支持
- 文件权限问题

**解决方法**：

1. **检查文件路径**
   - 在控制台查看 `file:///` URL 是否正确
   - 确保路径没有特殊字符问题

2. **测试图片格式**
   - 建议使用 JPG 或 PNG 格式
   - 避免使用 BMP 或其他特殊格式

3. **检查文件权限**
   - 确保壁纸文件可读
   - 尝试将壁纸复制到 `C:\Windows\Web\Wallpaper\` 目录

---

### 问题 3: 中文路径乱码

**症状**：壁纸路径包含中文时无法正确读取

**解决方法**：
- ✅ **已修复**：更新后的代码使用 `chcp 65001` 切换到 UTF-8 代码页
- 如果仍有问题，确保已经拉取最新代码

---

### 问题 4: 内存占用过高

**症状**：长时间运行后内存占用持续增长

**排查步骤**：

1. **打开性能监控**
   - 开发者工具 → Performance
   - 录制 10 次壁纸切换的性能数据

2. **检查内存泄漏**
   ```javascript
   // 在控制台运行
   performance.memory
   ```

3. **重启应用**
   - 如果内存占用超过 500MB，建议重启

---

## 📊 性能测试

### 测试内存使用

在开发者工具 Console 中运行：

```javascript
// 查看当前内存使用
console.log(performance.memory)

// 输出示例：
// {
//   jsHeapSizeLimit: 2172649472,
//   totalJSHeapSize: 89123456,
//   usedJSHeapSize: 67234567
// }
```

**健康指标**：
- `usedJSHeapSize` < 100MB：正常
- `usedJSHeapSize` 100-200MB：可接受
- `usedJSHeapSize` > 200MB：可能有内存泄漏

---

### 测试动画性能

1. 打开开发者工具 → **Performance** 标签
2. 点击 **录制** 按钮 (⭕)
3. 更改系统壁纸，等待应用更新
4. 停止录制
5. 查看 FPS 图表

**健康指标**：
- 动画期间 FPS 应该保持在 **55-60**
- 没有明显的长帧（red bars）

---

## 🎯 测试检查清单

复制此清单用于系统测试：

```
## 环境准备
[ ] Node.js 已安装
[ ] 依赖已安装 (npm install)
[ ] Windows 10/11 系统

## 启动测试
[ ] Vite 开发服务器启动成功
[ ] Electron 应用启动成功
[ ] 应用界面正常显示

## 功能测试
[ ] 能进入桌面模式
[ ] 能打开设置面板
[ ] 能找到壁纸同步开关

## 壁纸同步测试
[ ] 启用壁纸同步开关
[ ] 首次启用时立即显示系统壁纸
[ ] 更改系统壁纸后 10 秒内自动更新
[ ] 壁纸切换有流畅的动画效果
[ ] 连续切换 5 次壁纸都能正常检测
[ ] 关闭壁纸同步后恢复默认背景

## 边界情况测试
[ ] 中文路径壁纸正常显示
[ ] 长文件名壁纸正常显示
[ ] 特殊字符路径（括号、空格）正常显示
[ ] Wallpaper Engine 壁纸显示（如已安装）

## 性能测试
[ ] 长时间运行无内存泄漏
[ ] 动画性能良好（60 FPS）
[ ] 切换流畅，无卡顿

## 调试测试
[ ] 主进程日志正常输出
[ ] 渲染进程日志正常输出
[ ] test-wallpaper.mjs 脚本测试通过
[ ] 开发者工具能正常打开

## 持久化测试
[ ] 关闭应用后重启，设置保持
[ ] 壁纸同步状态正确保存
```

---

## 🚀 进一步的测试

### 自定义壁纸功能测试

虽然主要测试的是系统壁纸同步，但也可以测试自定义壁纸功能：

1. 在设置中点击 **"自定义壁纸"** 展开子菜单
2. 上传 1-6 张图片
3. 选择播放模式（单一/顺序/随机）
4. 选择切换模式（手动/定时/启动时）
5. 验证功能是否正常

---

## 📝 测试报告模板

```markdown
## WaveForge 壁纸同步功能测试报告

**测试日期**：YYYY-MM-DD
**测试人员**：[你的名字]
**系统环境**：Windows 11
**Node 版本**：v18.x.x

### 测试结果

- [ ] 通过 / [ ] 失败

### 测试详情

#### 1. 基本功能
- 壁纸同步开关：✅ / ❌
- 自动检测：✅ / ❌
- 动画效果：✅ / ❌

#### 2. 中文路径支持
- 测试路径：[实际路径]
- 结果：✅ / ❌

#### 3. 性能指标
- 内存使用：[XX] MB
- FPS：[XX]
- 响应延迟：[XX] 秒

### 发现的问题

1. [问题描述]
   - 重现步骤：
   - 预期结果：
   - 实际结果：
   - 严重程度：高/中/低

### 建议

[你的建议]
```

---

## 🎉 测试成功标准

如果以下所有项目都通过，则认为壁纸同步功能测试成功：

✅ 应用能正常启动
✅ 能进入桌面模式
✅ 壁纸同步开关能正常切换
✅ 启用后能立即显示系统壁纸
✅ 系统壁纸变化后 10 秒内自动更新
✅ 切换动画流畅自然
✅ 中文路径壁纸正常显示
✅ 关闭同步后恢复默认背景
✅ 无内存泄漏
✅ 无控制台错误

---

**祝测试顺利！如有问题，请查看日志并参考调试方法。** 🎊
