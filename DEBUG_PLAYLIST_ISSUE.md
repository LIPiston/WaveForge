# 歌单推荐显示0首问题调试指南

## 问题描述
歌单推荐功能中，每个歌单显示"0首"，数据不正确。

## 已添加的调试功能

### 前端调试日志 (HomeView.tsx:695-727)
在浏览器控制台中会显示：
- 🔍 QQ音乐歌单推荐数据结构
- 前3个歌单的完整JSON数据
- 每个歌单的所有字段名
- 各种可能的歌曲数量字段值 (song_cnt, songNum, song_count等)
- 解析后的trackCount值

### 后端调试日志 (local-server.mjs:1757-1758)
在服务器控制台中会显示：
- [QQ音乐推荐歌单] 第一个歌单的字段
- [QQ音乐推荐歌单] 第一个歌单完整JSON数据

## 调试步骤

### 1. 启动后端服务器
```bash
cd D:\opencode\WaveForge
node local-server.mjs
```
观察服务器控制台输出，查看：
- 是否成功启动
- QQ音乐API是否正常响应

### 2. 启动前端开发服务器
在新的终端窗口：
```bash
cd D:\opencode\WaveForge
npm run dev
```

### 3. 打开浏览器
访问 http://localhost:5173 (或vite显示的端口)

### 4. 打开浏览器开发者工具
- Chrome/Edge: F12 或 右键 -> 检查
- 切换到 Console (控制台) 标签

### 5. 导航到首页歌单推荐
- 确保已登录QQ音乐账号（如果需要）
- 查看"歌单推荐"模块

### 6. 查看调试输出

#### 在浏览器控制台查找：
```
🔍 QQ音乐歌单推荐数据结构:
  - data: {...}
  - data.data: {...}
  - data.data.list: [...]
  
🔍 第1个歌单的完整数据: {...}
🔍 第1个歌单的所有字段: [...]
🔍 可能的歌曲数量字段:
  - song_cnt: 未知
  - songNum: 未知
  - song_count: 未知
  ...
🔍 第1个歌单解析后的trackCount: 未知
```

#### 在服务器控制台查找：
```
[QQ音乐推荐歌单] 第一个歌单的字段: [...]
[QQ音乐推荐歌单] 第一个歌单示例: {...}
```

## 需要收集的信息

### 关键信息1: 实际的字段名
从调试输出中找到实际包含歌曲数量的字段名称。例如：
- 可能是 `song_cnt: 50`
- 可能是 `songNum: 50`
- 或者其他字段名

### 关键信息2: 数据结构
确认后端返回的数据结构是：
- `data.data.list` 还是
- `data.list`

### 关键信息3: 所有字段
记录完整的字段列表，可能包含我们没有考虑到的字段名。

## 下一步

找到正确的字段名后，我们需要修改 `HomeView.tsx:714` 行的代码：

```typescript
trackCount: item.正确的字段名 || 0,
```

## 常见问题

### Q: 看不到任何调试输出
A: 
1. 确认浏览器控制台已打开
2. 确认没有被过滤器隐藏（不要过滤日志）
3. 刷新页面重新加载

### Q: 服务器报错
A: 检查 local-server.mjs 是否正常启动，端口3001是否被占用

### Q: 前端无法连接后端
A: 确认后端服务运行在 http://localhost:3001

## 已尝试的字段名
根据代码，我们已经尝试了这些字段：
- song_cnt
- songnum
- song_num
- songNum
- song_count

如果这些都不对，调试输出会告诉我们正确的字段名。
