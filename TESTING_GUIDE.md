# 壁纸联动功能测试指南

## 快速开始

### 1. 启动开发环境

打开两个终端窗口：

**终端 1 - 启动前端开发服务器：**
```bash
cd D:\opencode\WaveForge
npm run dev
```

**终端 2 - 启动 Electron 桌面应用：**
```bash
cd D:\opencode\WaveForge
npm run dev:electron
```

### 2. 测试步骤

#### 步骤 1：进入桌面模式
1. 应用启动后，将鼠标移到屏幕最上方
2. 会出现一个下拉按钮，点击它
3. 选择"桌面"模式

#### 步骤 2：打开设置面板
1. 将鼠标移到屏幕底部，会出现一个小白条
2. 小白条会展开显示歌单轮播和三个按钮
3. 点击最右边的"设置"按钮（齿轮图标）

#### 步骤 3：启用壁纸同步
1. 在设置面板中找到"壁纸联动"部分
2. 找到"Wallpaper Engine 同步"开关
3. 点击开关，启用壁纸同步功能
4. 应用背景应该立即变为您的系统桌面壁纸

#### 步骤 4：测试壁纸变化检测
1. 保持应用打开
2. 右键桌面 → 个性化 → 背景
3. 更换您的桌面壁纸
4. 等待最多 10 秒
5. 应用中的背景应该自动更新为新壁纸

### 3. 预期效果

✅ **成功的表现：**
- 启用同步后，背景立即显示系统壁纸
- 壁纸切换时有平滑的淡入淡出动画
- 控制台显示：`✅ 成功获取系统壁纸: file:///C:/Users/.../wallpaper.jpg`
- 更换系统壁纸后，应用背景自动跟随变化

❌ **可能的问题：**
- 如果看到错误信息，检查控制台（按 F12）
- 如果壁纸不显示，确认系统已设置桌面壁纸
- 如果壁纸不更新，等待 10 秒让系统检测

### 4. 调试技巧

#### 查看控制台日志
按 `F12` 打开开发者工具，在 Console 标签中查看：
- `✅ 成功获取系统壁纸:` - 表示成功获取
- `🖼️ 系统壁纸已变化:` - 表示检测到壁纸变化
- `❌ 获取系统壁纸失败:` - 表示出现错误

#### 手动测试 API
在控制台中运行：
```javascript
// 测试获取壁纸
const result = await window.electron.wallpaper.getCurrentWallpaper()
console.log(result)

// 测试监听壁纸变化
window.electron.wallpaper.onWallpaperChange((path) => {
  console.log('壁纸变化:', path)
})
```

### 5. 其他测试场景

#### 测试场景 A：多个壁纸文件
1. 准备几张不同的壁纸图片
2. 在系统设置中频繁切换壁纸
3. 观察应用中的背景是否能正确跟随

#### 测试场景 B：与自定义壁纸配合
1. 在设置中点击"自定义壁纸"
2. 上传一些自己的壁纸图片
3. 关闭"Wallpaper Engine 同步"
4. 测试自定义壁纸是否正常工作
5. 重新启用"Wallpaper Engine 同步"
6. 确认系统壁纸优先级更高

#### 测试场景 C：应用重启
1. 启用壁纸同步
2. 关闭应用
3. 更换系统壁纸
4. 重新打开应用
5. 检查是否显示最新的系统壁纸

### 6. 已知限制

- 仅支持 Windows 系统
- 检测间隔为 10 秒（可能有延迟）
- Wallpaper Engine 动态壁纸会显示为静态图片
- 某些特殊格式的壁纸可能不支持

### 7. 性能测试

观察以下指标：
- CPU 使用率（应该很低，每 10 秒仅执行一次检测）
- 内存占用（壁纸切换时应该没有内存泄漏）
- 切换动画流畅度（应该是平滑的 60fps）

## 故障排查

### 问题 1：壁纸无法显示
**可能原因：**
- 系统没有设置桌面壁纸
- 文件路径包含特殊字符
- PowerShell 执行权限问题

**解决方法：**
1. 确认桌面有壁纸
2. 检查控制台错误信息
3. 尝试使用简单路径的壁纸文件

### 问题 2：壁纸不自动更新
**可能原因：**
- 壁纸同步开关未启用
- 10 秒检测间隔尚未到达
- 注册表未更新

**解决方法：**
1. 确认开关已启用
2. 等待至少 10 秒
3. 尝试重启应用

### 问题 3：Electron 窗口无法打开
**可能原因：**
- 前端服务器未启动
- 端口被占用

**解决方法：**
1. 确保先启动 `npm run dev`
2. 检查 3000 端口是否可用
3. 查看 Electron 进程的错误信息

## 成功标准

测试通过的标准：
- ✅ 能够成功启用壁纸同步
- ✅ 背景立即显示系统壁纸
- ✅ 更换系统壁纸后，应用背景自动更新（10秒内）
- ✅ 壁纸切换动画流畅
- ✅ 没有内存泄漏
- ✅ 控制台无错误信息

---

**测试完成后，请将结果反馈给开发团队！**
