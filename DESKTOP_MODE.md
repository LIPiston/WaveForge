# WaveForge 桌面模式

## 概述

WaveForge 提供两种并行的显示模式：

- **简约模式**：沉浸式音乐播放体验，专注听歌。
- **桌面模式**：以歌单为中心的桌面级音乐管理体验，带 3D 歌单轮播、独立平台切换和壁纸联动。

两种模式状态互相隔离、互不干扰，可随时切换。

## 切换模式

1. 点击界面顶部的下拉箭头（↓），弹出「模式选择」面板。
2. 选择「桌面」或「简约」，界面即时切换。
3. 当前模式会保存在 `localStorage`（`viewMode`），下次启动自动恢复上次使用的模式。

## 桌面模式功能

### 1. 3D 歌单轮播（PlaylistCarousel3D）

- 3D 透视效果展示歌单，支持 3D 旋转、渐进模糊与缩放。
- 交互方式：
  - **鼠标滚轮**：上下滚动切换歌单。
  - **触摸滑动**：触摸屏左右滑动。
  - **键盘**：`←` / `→` 切换歌单，`Enter` 打开当前歌单。
  - **点击卡片**：点击非中心卡片快速定位，点击中心卡片打开歌单详情。

### 2. 歌单详情视图

- 左侧为歌单封面、创建者信息与「播放全部」按钮；右侧为可滚动歌曲列表。
- 点击歌曲立即播放；显示时长、艺术家、专辑等信息。
- 会话级记忆滚动位置与上次播放位置。
- 进入/退出有缩放 + 淡入淡出动画，列表项带瀑布流延迟动画。

### 3. 底部控制栏（小白条）

- 点击小白条展开/收起「控制药丸」，悬停有放大效果。
- 控制药丸包含：**平台切换**（网易云 / QQ 音乐）、**搜索**、**设置**（桌面模式专用）。

### 4. 平台切换（桌面模式独占）

- 桌面模式拥有独立的平台选择，切换「网易云」/「QQ 音乐」不影响简约模式。
- 切换后自动加载对应平台的用户歌单；选择保存在 `localStorage`（`desktopModePlatform`）。
- 需要先登录对应平台账号，未登录时会显示提示。

### 5. 桌面模式设置（居中弹窗）

- **壁纸联动**：Wallpaper Engine 同步开关。详见 [WALLPAPER_GUIDE.md](./WALLPAPER_GUIDE.md)。
- **歌单显示**：卡片大小（小/中/大）、同时显示歌单数（3-7 个）。
- **播放控制**：自动播放下一首。
- 设置面板为居中弹窗 + 玻璃态背景（区别于简约模式的侧边栏）。

### 6. 搜索

- 沿用简约模式的搜索面板，支持跨平台搜索，结果可直接播放。

## 桌面模式 vs 简约模式

| 功能 | 简约模式 | 桌面模式 |
|------|---------|---------|
| 界面风格 | 沉浸式播放器 | 歌单管理器 |
| 主要交互 | 歌曲播放控制 | 歌单浏览和管理 |
| 平台切换 | 全局统一 | 独立设置 |
| 设置面板 | 右侧滑出 | 居中弹窗 |
| 歌词大字号显示 | ✅ | ❌（专注歌单） |
| 封面律动 | ✅ | ❌ |
| Wallpaper Engine | ❌ | ✅ |
| 3D 歌单轮播 | ❌ | ✅ |
| 适用场景 | 专注听歌 | 浏览和选择音乐 |

## 技术实现要点（简洁）

- **核心组件**：`DesktopView.tsx`（主组件）、`PlaylistCarousel3D.tsx`（3D 轮播）、`PlaylistDetailView.tsx`（详情）、`DesktopSettingsModal.tsx`（设置）。
- **动画**：统一使用 Framer Motion；轮播为弹簧动画（刚度 300、阻尼 30），详情视图为缩放 + 淡入淡出，列表项瀑布流延迟。
- **状态管理**：平台选择、卡片大小、壁纸开关等桌面模式设置独立于简约模式；登录状态、播放控制、音频状态等共享。
- **数据持久化**：`localStorage` 保存 `viewMode`、`desktopModePlatform`、`desktopPlaylistCardSize`、`wallpaperSyncEnabled` 等；`sessionStorage` 记忆歌单滚动位置与上次播放位置。
- **性能优化**：歌单封面懒加载、滚轮事件防抖、键盘事件节流、组件卸载时清理定时器与动画。

## 已知限制

- **移动端**：主要为桌面端设计，移动端可用但体验待优化。
- **歌词与封面律动**：桌面模式不显示大字号歌词和封面律动，专注歌单浏览。
- **壁纸联动**：依赖 Windows 系统与 Electron 主进程，详见 [WALLPAPER_GUIDE.md](./WALLPAPER_GUIDE.md) 的已知限制。
- **歌单显示**：需登录对应平台账号且账号内有创建或收藏的歌单，否则不显示。
- **动画性能**：歌单数量过多或卡片过大会影响流畅度，可通过减少显示数量、缩小卡片或关闭壁纸同步缓解。
