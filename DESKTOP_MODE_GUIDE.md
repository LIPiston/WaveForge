# WaveForge 桌面模式使用指南

## 概述

WaveForge 现在支持两种显示模式：
- **简约模式**：专注于音乐播放的沉浸式体验
- **桌面模式**：歌单为中心的桌面级音乐管理体验

## 切换模式

### 方法一：通过首页切换
1. 点击顶部的下拉箭头（↓）
2. 在弹出的"模式选择"面板中选择"桌面"
3. 界面将自动切换到桌面模式

### 方法二：通过设置切换
1. 模式选择在首次启动或通过顶部下拉菜单触发
2. 当前模式会被保存，下次启动自动使用上次的模式

## 桌面模式功能

### 1. 歌单轮播 (Carousel3D)

**特点：**
- 3D 透视效果的歌单展示
- 支持多种交互方式

**操作方式：**
- **鼠标滚轮**：上下滚动切换歌单
- **触摸滑动**：在触摸屏上左右滑动
- **键盘方向键**：← → 切换歌单
- **点击卡片**：点击非中心卡片可快速定位，点击中心卡片打开歌单详情

**动画参数：**
- 弹簧动画：刚度 300，阻尼 30
- 3D 旋转：±15° Y轴旋转
- 模糊效果：0-4px 渐进式模糊
- 缩放范围：0.7-1.1x

### 2. 歌单详情视图

**布局：**
- 左侧：歌单封面、创建者信息、播放全部按钮
- 右侧：可滚动的歌曲列表

**功能：**
- 点击歌曲立即播放
- 记忆滚动位置（会话级）
- 记忆上次播放位置
- 显示歌曲时长、艺术家、专辑信息

**动画效果：**
- 进入：缩放 + 淡入动画
- 歌曲列表：瀑布流延迟动画（每项延迟 20ms）
- 退出：缩放 + 淡出动画

### 3. 底部控制栏

**小白条功能：**
- 点击展开/收起控制药丸
- 悬停时轻微放大效果

**控制药丸包含：**
- **平台切换按钮**：在网易云音乐和QQ音乐之间切换（仅桌面模式）
- **搜索按钮**：打开搜索面板
- **设置按钮**：打开桌面模式专用设置

### 4. 平台切换（桌面模式独占）

**功能说明：**
- 桌面模式有独立的平台选择，不影响简约模式
- 点击平台按钮在"网易云"和"QQ音乐"之间切换
- 切换后自动加载对应平台的用户歌单
- 平台选择会被保存，下次进入桌面模式时自动恢复

**注意事项：**
- 需要先登录对应平台账号
- 未登录时会显示提示信息

### 5. 桌面模式设置

**设置项：**

#### 壁纸联动
- **Wallpaper Engine 同步**：将 Wallpaper Engine 的桌面壁纸作为背景
- 需要 Wallpaper Engine 运行并启用 Web API
- 详见 [WALLPAPER_ENGINE_INTEGRATION.md](./WALLPAPER_ENGINE_INTEGRATION.md)

#### 歌单显示
- **歌单卡片大小**：小（200px）/ 中（240px）/ 大（280px）
- **同时显示歌单数**：3-7 个可见歌单

#### 播放控制
- **自动播放下一首**：歌曲结束后自动播放列表中的下一首

**设置面板样式：**
- 居中弹窗设计（区别于简约模式的侧边栏）
- 玻璃态背景效果
- 圆角卡片布局

### 6. 搜索功能

- 沿用简约模式的搜索面板
- 支持跨平台搜索
- 搜索结果直接播放

## 桌面模式 vs 简约模式

| 功能 | 简约模式 | 桌面模式 |
|------|---------|---------|
| **界面风格** | 沉浸式播放器 | 歌单管理器 |
| **主要交互** | 歌曲播放控制 | 歌单浏览和管理 |
| **平台切换** | 全局统一 | 独立设置 |
| **设置面板** | 右侧滑出 | 居中弹窗 |
| **歌词显示** | ✅ 大字号显示 | ❌ 专注歌单 |
| **封面律动** | ✅ 支持 | ❌ 不支持 |
| **Wallpaper Engine** | ❌ 不支持 | ✅ 支持 |
| **歌单轮播** | ❌ | ✅ 3D 轮播 |
| **适用场景** | 专注听歌 | 浏览和选择音乐 |

## 技术细节

### 动画库
使用 **Framer Motion** 实现所有动画效果

### 核心组件

```
src/components/
├── DesktopView.tsx              # 桌面模式主组件
├── PlaylistCarousel3D.tsx       # 3D 歌单轮播
├── PlaylistDetailView.tsx       # 歌单详情视图
└── DesktopSettingsModal.tsx     # 桌面模式设置
```

### 状态管理

**独立状态（不共享）：**
- 当前平台选择（`desktopModePlatform`）
- 歌单卡片大小（`desktopPlaylistCardSize`）
- 壁纸同步开关（`wallpaperSyncEnabled`）

**共享状态：**
- 登录状态
- 当前播放歌曲
- 音频控制

### 数据持久化

使用 localStorage 保存：
- `viewMode`：当前视图模式
- `desktopModePlatform`：桌面模式平台选择
- `desktopPlaylistCardSize`：歌单卡片大小
- `wallpaperSyncEnabled`：壁纸同步开关
- `currentWallpaperPath`：当前壁纸路径

使用 sessionStorage 保存：
- `folia_scroll_playlist_${id}`：歌单滚动位置
- `folia_playlist_last_index_${id}`：上次播放位置

## 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `←` | 切换到上一个歌单 |
| `→` | 切换到下一个歌单 |
| `Enter` | 打开当前聚焦的歌单 |
| `Esc` | 关闭当前打开的面板 |

## 常见问题

### Q: 如何在两个模式之间快速切换？
A: 点击顶部的下拉箭头（↓），在模式选择面板中切换。

### Q: 桌面模式的平台选择会影响简约模式吗？
A: 不会，两个模式的平台选择是独立的。

### Q: 为什么我的歌单没有显示？
A: 请确保：
1. 已登录对应平台账号
2. 选择的平台与登录的账号一致
3. 账号中有创建或收藏的歌单

### Q: Wallpaper Engine 同步不工作？
A: 请参考 [WALLPAPER_ENGINE_INTEGRATION.md](./WALLPAPER_ENGINE_INTEGRATION.md) 完成集成配置。

### Q: 歌单切换动画卡顿？
A: 尝试：
1. 减少同时显示的歌单数量
2. 选择较小的卡片大小
3. 关闭 Wallpaper Engine 同步
4. 检查系统性能

### Q: 如何恢复默认设置？
A: 在桌面模式设置中，每个设置项都可以单独调整。如需完全重置，可以清除浏览器的 localStorage。

## 更新日志

### v0.2.0 - 桌面模式发布
- ✅ 新增桌面模式
- ✅ 实现 3D 歌单轮播（参考 folia-major）
- ✅ 独立的平台切换功能
- ✅ 居中弹窗式设置面板
- ✅ Wallpaper Engine 集成框架
- ✅ 歌单详情视图（复刻 folia 动画）
- ✅ 模式独立性保证

## 反馈与建议

如遇到问题或有功能建议，请通过以下方式反馈：
- 在项目中创建 Issue
- 提供详细的复现步骤和截图

---

**享受音乐，享受 WaveForge！** 🎵
