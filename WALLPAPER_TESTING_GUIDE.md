# WaveForge 壁纸同步功能测试指南

## 📋 测试概述

本指南将帮助你全面测试 WaveForge 的壁纸同步功能（桌面模式）。

---

## 🎯 测试前准备

### 1. 环境要求
- **操作系统**: Windows 10 或 Windows 11
- **Node.js**: v16 或更高版本
- **权限**: 管理员权限（用于读取注册表）

### 2. 安装依赖
```bash
cd D:\opencode\waveforge
npm install
```

### 3. 检查项目是否正常构建
```bash
npm run lint
```

---

## 🚀 测试方法

### 方法一：开发模式测试（推荐）

#### 步骤 1: 启动 Vite 开发服务器
打开第一个终端：
```bash
npm run dev
```
✅ 应该看到：`Local: http://127.0.0.1:3000/`

#### 步骤 2: 启动 Electron 应用
打开第二个终端：
```bash
npm run dev:electron
```
✅ 应该看到 Electron 窗口打开，显示 WaveForge 界面

#### 步骤 3: 进入桌面模式
- 在应用界面中，点击右上角的"桌面模式"按钮
- 或者找到切换视图的图标

#### 步骤 4: 打开设置面板
- 点击右上角的齿轮图标（⚙️）
- 设置面板应该滑出

#### 步骤 5: 启用壁纸同步
- 在设置面板中找到 **"Wallpaper Engine 同步"** 选项
- 将开关切换到 **ON** 状态
- 应该看到开关变为绿色/激活状态

#### 步骤 6: 测试自动检测
1. 打开 Windows 设置 → 个性化 → 背景
2. 更改桌面壁纸到任意图片
3. 等待 **最多 10 秒**
4. 观察 WaveForge 应用的背景是否自动更新

✅ **预期结果**：
- 背景应该平滑过渡到新壁纸
- 有淡入淡出动画效果（0.6秒）
- 控制台应该输出：`Wallpaper changed via Electron: file:///...`

---

### 方法二：生产模式测试

#### 构建 Electron 应用
```bash
npm run build:electron
```

#### 运行打包后的应用
构建完成后，在 `release/` 目录下找到安装程序：
```
D:\opencode\waveforge\release\WaveForge-0.1.0-Setup.exe
```

安装并运行，然后重复上述测试步骤 3-6。

---

## 🧪 详细测试用例

### 测试用例 1: 基本壁纸同步
**步骤**:
1. 启动应用（开发模式）
2. 进入桌面模式
3. 启用壁纸同步
4. 更改 Windows 壁纸

**预期结果**:
- ✅ 应用背景在 10 秒内更新
- ✅ 过渡动画流畅
- ✅ 图片清晰，无拉伸变形

---

### 测试用例 2: 多次切换壁纸
**步骤**:
1. 连续更改 Windows 壁纸 3-5 次
2. 每次等待 10 秒观察

**预期结果**:
- ✅ 每次都能正确检测并更新
- ✅ 不会出现内存泄漏或卡顿
- ✅ 动画每次都正常播放

---

### 测试用例 3: 关闭壁纸同步
**步骤**:
1. 启用壁纸同步后，更改 Windows 壁纸
2. 等待应用背景更新
3. 在设置中关闭壁纸同步
4. 再次更改 Windows 壁纸

**预期结果**:
- ✅ 关闭后，应用不再响应系统壁纸变化
- ✅ 应用显示默认的液态玻璃背景（LiquidGlassBackground）

---

### 测试用例 4: 自定义壁纸功能
**步骤**:
1. 在设置中点击"自定义壁纸"
2. 上传 1-6 张图片（支持 jpg, png, webp）
3. 选择播放模式（单一/顺序/随机）
4. 选择切换模式（手动/定时/启动时）
5. 如果选择定时，设置间隔（10/30/60 分钟或自定义）

**预期结果**:
- ✅ 图片上传成功，显示缩略图
- ✅ 每张图片最大 50MB
- ✅ 最多可上传 6 张图片
- ✅ 切换模式按预期工作
- ✅ 定时切换准确触发

---

### 测试用例 5: 随机 API 壁纸
**步骤**:
1. 在设置中选择"随机图片来源"
2. 选择一个来源（Bing 每日/风景/动漫/自定义 API）
3. 设置为"启动时切换"或"定时切换"
4. 重启应用或等待定时触发

**预期结果**:
- ✅ 能从 API 获取随机图片
- ✅ 图片加载成功并显示
- ✅ 网络错误时有适当的回退机制

---

### 测试用例 6: Wallpaper Engine 支持
**前置条件**: 已安装 Wallpaper Engine

**步骤**:
1. 在 Wallpaper Engine 中设置一个壁纸（包括动态壁纸）
2. 在 WaveForge 中启用壁纸同步
3. 观察应用背景

**预期结果**:
- ✅ 静态壁纸能正常显示
- ⚠️ 动态壁纸会显示为静态截图（这是已知限制）

**注意**: 完整的动态壁纸支持计划在未来版本实现。

---

## 🐛 调试方法

### 查看 Electron 控制台日志

#### 主进程日志
在运行 `npm run dev:electron` 的终端中查看输出。

关键日志：
```
获取壁纸路径失败: [错误信息]
壁纸监听出错: [错误信息]
```

#### 渲染进程日志
在 Electron 窗口中按 `F12` 或 `Ctrl+Shift+I` 打开开发者工具。

关键日志：
```javascript
Wallpaper changed via Electron: file:///C:/Users/.../wallpaper.jpg
```

---

### 手动测试 PowerShell 命令

打开 PowerShell，运行：
```powershell
$wallpaperPath = Get-ItemPropertyValue -Path 'HKCU:\Control Panel\Desktop' -Name Wallpaper
Write-Output $wallpaperPath
```

✅ 应该输出当前壁纸的完整路径，例如：
```
C:\Users\YourName\Pictures\wallpaper.jpg
```

如果没有输出或报错，说明系统壁纸设置可能有问题。

---

### 检查注册表

打开注册表编辑器（`regedit`）：
```
HKEY_CURRENT_USER\Control Panel\Desktop
```

查找键：`Wallpaper`

✅ 值应该是当前壁纸的完整路径。

---

## 📊 性能测试

### 内存使用监控

#### 在开发者工具中
1. 打开 Performance Monitor
2. 运行 `performance.memory`
3. 切换壁纸 10 次
4. 检查内存是否持续增长

✅ **预期**: 内存使用应该稳定，不会无限增长

---

### 动画性能测试

#### 检查帧率
1. 打开开发者工具 → Performance 标签
2. 开始录制
3. 切换壁纸
4. 停止录制并分析

✅ **预期**: 动画应该保持 60 FPS，无明显掉帧

---

## 🔧 常见问题排查

### 问题 1: 壁纸不自动更新
**可能原因**:
- 壁纸同步开关未开启
- 系统不是 Windows 10/11
- PowerShell 被禁用或受限
- 壁纸文件路径包含特殊字符

**解决方法**:
1. 检查开关状态
2. 查看控制台错误日志
3. 手动运行 PowerShell 命令测试
4. 更改壁纸到标准路径（如 `C:\Windows\Web\Wallpaper\`）

---

### 问题 2: 图片显示异常
**可能原因**:
- 图片路径转换错误
- 图片格式不支持
- 文件权限问题

**解决方法**:
1. 检查控制台中的 `file:///` URL 是否正确
2. 尝试更换为常见格式（jpg/png）
3. 确保图片文件可读

---

### 问题 3: 延迟过长
**可能原因**:
- 10 秒轮询间隔

**解决方法**:
- 这是设计行为，未来版本可能支持配置
- 可以在 `desktop/main.js:115` 修改间隔时间（单位：毫秒）

---

### 问题 4: 自定义壁纸上传失败
**可能原因**:
- 文件超过 50MB
- 已上传 6 张图片
- 文件格式不支持

**解决方法**:
1. 压缩图片大小
2. 删除不需要的壁纸腾出空间
3. 使用支持的格式（jpg, png, webp, gif）

---

## 📝 测试检查清单

复制以下清单用于测试记录：

```
[ ] 开发环境启动成功
[ ] 进入桌面模式
[ ] 打开设置面板
[ ] 启用壁纸同步开关
[ ] 自动检测系统壁纸变化（10秒内）
[ ] 壁纸切换动画流畅
[ ] 多次切换壁纸正常
[ ] 关闭壁纸同步后恢复默认背景
[ ] 上传自定义壁纸成功
[ ] 自定义壁纸播放模式工作正常
[ ] 定时切换功能正常
[ ] 随机 API 壁纸获取成功
[ ] Wallpaper Engine 壁纸显示正常
[ ] 无内存泄漏
[ ] 动画性能良好（60 FPS）
[ ] 控制台无错误日志
[ ] 生产模式打包成功
[ ] 安装包正常运行
```

---

## 🎓 深入理解

### 代码结构
```
壁纸同步流程:
Windows 注册表
    ↓ (PowerShell 每 10 秒查询)
Electron 主进程 (desktop/main.js)
    ↓ (IPC 通信)
Preload 脚本 (desktop/preload.cjs)
    ↓ (安全 API)
前端组件 (DesktopView.tsx)
    ↓ (状态管理)
渲染层 (带动画的背景图)
```

### 关键文件位置
- **主进程**: `desktop/main.js:45-133`
- **Preload**: `desktop/preload.cjs:17-22`
- **壁纸管理器**: `src/services/desktopWallpaperManager.ts`
- **桌面视图**: `src/components/DesktopView.tsx:111-220`
- **设置面板**: `src/components/DesktopSettingsModal.tsx:312-369`

---

## 📞 需要帮助？

如果测试中遇到问题：

1. **查看日志**: 主进程终端 + 渲染进程开发者工具
2. **检查文档**: 项目根目录下的 `WALLPAPER_SYNC_README.md`
3. **查看源码**: 关键文件都有详细注释

---

## 🚀 下一步

测试完成后，可以：
1. 优化轮询间隔（修改 `desktop/main.js:115`）
2. 添加实时文件监听（使用 `chokidar` 等库）
3. 支持视频壁纸播放
4. 支持多显示器壁纸选择
5. 添加壁纸缓存机制

---

**测试愉快！🎉**
