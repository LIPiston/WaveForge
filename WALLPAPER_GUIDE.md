# WaveForge 壁纸功能指南

## 功能概述

WaveForge 桌面模式提供完整的壁纸联动能力，可以把桌面背景变成播放器背景：

- **系统壁纸同步**：启用后自动显示 Windows 系统桌面壁纸，并在系统壁纸变化时自动跟随更新。
- **Wallpaper Engine 联动**：支持检测 Wallpaper Engine 的当前壁纸，视频/网页类型壁纸可直接提取视频作为背景。
- **自定义壁纸**：支持上传本地图片和视频，按播放模式（单张/顺序/随机）与切换模式（手动/定时/启动时）循环展示。
- **随机 API 壁纸**：从内置图片源或自定义 API 获取随机图片作为背景。
- **默认背景**：未启用任何壁纸时显示深紫蓝渐变动态背景，可随时一键重置回默认。

## 实现方案（简洁）

壁纸同步链路：`系统壁纸/ Wallpaper Engine` → `Electron 主进程` → `IPC` → `Preload API` → `前端组件`。

1. **主进程 `desktop/main.cjs`**
   - 通过 `reg.exe` 查询注册表 `HKCU\Control Panel\Desktop\Wallpaper` 获取系统壁纸路径；输出用 GBK 解码，因此中文路径可正常读取。
   - 通过 PowerShell 定位 Wallpaper Engine 安装目录，读取其 `config.json` 获取当前壁纸；视频/网页类型壁纸会提取其中的视频文件作为背景，场景（Scene）壁纸不支持时自动回退到系统壁纸。
   - 每 10 秒轮询一次壁纸签名（路径 + 修改时间 + 大小），发生变化时通过 `wallpaper-changed` 事件推送给渲染进程。
2. **Preload `desktop/preload.cjs`**：暴露 `window.electron.wallpaper.getCurrentWallpaper()` 与 `onWallpaperChange(callback)` 两个安全 API。
3. **前端**：`src/services/desktopWallpaperManager.ts` 负责壁纸状态管理与 localStorage 持久化；`src/components/DesktopView.tsx` 应用壁纸并播放淡入淡出切换动画（约 0.6 秒）；`src/components/DesktopSettingsModal.tsx` 提供设置界面。
4. 相关文件还包括随机图片源配置（Bing/风景/动漫/自定义 API）与 Wallpaper Engine 轮换服务（`src/services/wallpaperEngineRotation.ts`）。

> **历史信息**：早期文档提出过三种 Wallpaper Engine 集成方案 —— Web API（端口 52025）、监听 `wallpaper_engine_config.json` 文件、自定义插件（WebSocket）。这些方案均为当时的设计稿，最终采用了解析 Wallpaper Engine 配置文件的直接方案，上述方案未被采用。

## 使用方法

### 启用系统壁纸同步

1. 进入桌面模式（顶部下拉 → 模式选择 → 桌面）。
2. 将鼠标移到屏幕底部，点击小白条展开控制栏，点击设置按钮（齿轮图标）。
3. 在「壁纸联动」区域开启 **"Wallpaper Engine 同步"** 开关。
4. 背景立即切换为系统桌面壁纸；更换系统壁纸后最多 10 秒内自动跟随。

> 开启开关后应用优先显示系统壁纸 / Wallpaper Engine 壁纸。需要先关闭该开关，才能手动选择自定义壁纸，二者互斥。

### 使用自定义壁纸

1. 在桌面模式设置中进入「自定义壁纸」。
2. 上传本地图片或视频：支持常见图片格式与视频，最多 12 个文件，单个不超过 1GB。
3. 选择播放模式：单张 / 顺序循环 / 随机循环。
4. 选择切换模式：手动 / 定时 / 启动时。选择「定时」时可设置间隔（10 / 30 / 60 分钟或自定义时长）。
5. 点击「重置为默认壁纸」可一键清空上传内容并恢复默认渐变背景。

### 使用随机 API 壁纸

1. 在设置中选择「随机图片来源」：Bing 每日 / 风景 / 动漫 / 自定义 API。
2. 选择「启动时切换」或「定时切换」，重启应用或等待定时触发。
3. 自定义 API 需填写返回图片的链接地址（如项目内置的风景/动漫源），保存并应用即可。

## 支持的系统

- ✅ Windows 10 / Windows 11
- ✅ 静态图片壁纸
- ✅ Wallpaper Engine 壁纸（视频/网页类型可提取视频；场景类型不支持时回退系统壁纸）
- ❌ macOS（暂不支持）
- ❌ Linux（暂不支持）

## 测试指南

以下用例合并自多份测试文档，按顺序执行即可覆盖主要功能。

### 测试准备

- 操作系统：Windows 10/11。
- 启动应用：按 README 中的启动方式运行（开发模式 `npm run dev:electron` 会自动构建并启动；生产模式构建后安装运行）。
- 依赖验证：`node test-wallpaper.mjs` 可独立验证注册表读取与中文路径处理逻辑。

### 功能测试步骤

1. **进入桌面模式**：顶部下拉切换「桌面」，确认 3D 歌单轮播界面出现。
2. **打开设置**：底部小白条 → 控制药丸 → 齿轮图标。
3. **启用壁纸同步**：开启「Wallpaper Engine 同步」，背景应立即变为系统壁纸。
4. **测试自动检测**：Windows 设置 → 个性化 → 背景，更换壁纸后等待最多 10 秒，应用背景应自动平滑过渡（淡入淡出约 0.6 秒）。
5. **关闭同步**：关闭开关后更换系统壁纸，应用不再响应，恢复默认渐变背景。

### 测试用例

| 用例 | 操作 | 预期结果 |
|------|------|---------|
| 基本同步 | 开启开关后更换系统壁纸 | 10 秒内自动更新，动画流畅、图片不变形 |
| 连续切换 | 连续更换壁纸 3-5 次 | 每次均正确检测，无卡顿、无内存泄漏 |
| 关闭同步 | 关闭后更换系统壁纸 | 不再响应，恢复默认背景 |
| 自定义壁纸 | 上传 1-12 个文件，设置播放/切换模式 | 缩略图正确、模式按预期工作、定时准确触发 |
| 随机 API | 选择随机图片来源并设置定时切换 | 能获取并显示图片，网络错误有回退 |
| Wallpaper Engine | 已安装 WE 并设置壁纸，开启同步 | 静态壁纸正常显示；视频/网页壁纸显示提取的视频；场景壁纸回退系统壁纸 |

### 调试方法

- **主进程日志**：运行 `npm run dev:electron` 的终端中查看，关键日志：`成功获取壁纸路径` / `获取壁纸路径失败` / `检测到壁纸变化`。
- **渲染进程日志**：窗口内按 F12 打开开发者工具，Console 中可见 `file:///` 形式的壁纸 URL 与变化事件。
- **手动验证注册表**：在 PowerShell 中运行 `Get-ItemPropertyValue -Path 'HKCU:\Control Panel\Desktop' -Name Wallpaper`，应输出当前壁纸完整路径（含中文也应正常）。
- **检查持久化**：开发者工具 Console 中读取 `localStorage`：`wallpaperSyncEnabled`（开关状态）、`desktop_wallpaper_settings`（壁纸设置 JSON）。

### 检查清单

```
[ ] 能进入桌面模式并打开设置
[ ] 启用同步后立即显示系统壁纸
[ ] 更换系统壁纸后 10 秒内自动更新
[ ] 切换动画流畅（60 FPS）
[ ] 连续切换多次正常，无内存泄漏
[ ] 关闭同步后恢复默认背景
[ ] 中文路径壁纸正常显示
[ ] 自定义壁纸上传与切换模式正常
[ ] 随机 API 壁纸获取成功
[ ] Wallpaper Engine 壁纸显示正常（如已安装）
[ ] test-wallpaper.mjs 通过
[ ] 重启后设置保持
```

## 故障排查

### 壁纸不显示或自动更新失败

- 确认处于桌面模式且「Wallpaper Engine 同步」开关已开启。
- 确认系统已设置桌面壁纸；检查主进程/渲染进程控制台有无错误日志。
- 手动运行 `Get-ItemPropertyValue -Path 'HKCU:\Control Panel\Desktop' -Name Wallpaper` 验证系统壁纸路径。
- 关闭并重新打开应用；仍不行时运行 `node test-wallpaper.mjs` 定位问题。

### 图片显示异常（空白或拉伸）

- 检查控制台中的 `file:///` URL 是否正确。
- 优先使用 JPG/PNG 等常见格式；确认图片文件存在且可读。

### Wallpaper Engine 同步不生效

- 确认 Wallpaper Engine 已安装并正在运行（当前通过进程定位安装目录并读取配置）。
- 场景（Scene）类型壁纸不受支持，会回退为系统壁纸；视频/网页类型应可正常提取视频。
- 尝试重启 WaveForge 或重新开启开关。

### 检测有延迟

- 10 秒轮询间隔是设计行为，用于避免频繁读取注册表影响性能，并非故障。

## 已知限制

- **检测延迟**：壁纸变化后最多等待 10 秒才更新。
- **平台限制**：仅支持 Windows 10/11；macOS、Linux 暂不支持。
- **路径限制**：仅支持本地文件路径，不支持网络路径（UNC）。
- **Wallpaper Engine**：场景（Scene）类型壁纸不支持，会自动回退系统壁纸；视频/网页类型壁纸支持提取视频播放。
- **环境依赖**：系统壁纸同步依赖 Electron 主进程；纯浏览器环境（仅运行 `npm run dev`）无法获取系统壁纸。
- **仅桌面模式可用**：壁纸联动属于桌面模式功能，简约模式不受影响。
