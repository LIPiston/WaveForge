# 低音量下的频响补偿技术报告

> 本报告梳理耳机、手机厂商在低音量回放时的频响补偿思路、算法路径、厂商实现方式与工程调整细节。由于各厂商具体算法多属商业机密，本文基于公开专利、白皮书、产品资料与行业通用 DSP 设计进行归纳。

---

## 目录

1. 背景：为什么低音量需要频响补偿  
2. 理论基础：等响曲线与目标曲线  
3. 典型处理链路  
4. 核心算法模块  
   4.1 音量/响度估计  
   4.2 补偿曲线计算  
   4.3 滤波器实现  
   4.4 动态范围控制与限幅  
   4.5 低频心理声学增强  
5. 各厂商实现概览  
   5.1 Apple  
   5.2 Samsung / Harman  
   5.3 Sony  
   5.4 Bose  
   5.5 Dolby / Audyssey / Dirac / Waves  
   5.6 手机 SoC 与 Codec 厂商  
6. 调整细节与参数示例  
7. 工程实现注意事项  
8. 总结  
9. 附录：示例伪代码  

---

## 1. 背景：为什么低音量需要频响补偿

人耳对声音响度的感知是非线性的。在低声压级下，人耳对低频和高频的灵敏度明显下降，尤其是低频。这意味着：

- 在正常音量下，一首歌的低音、高音与人声比例是平衡的；
- 当音量调低后，人耳会感觉低频几乎消失、高频细节变少、整体声音变薄；
- 如果不做补偿，用户会不自觉地调高音量，或者觉得音质变差。

因此，厂商在耳机和手机中加入“响度补偿 / 低频增强 / 动态 EQ”等功能，使低音量下仍能保持接近正常音量时的听感平衡。

---

## 2. 理论基础：等响曲线与目标曲线

频响补偿的核心依据是 **等响曲线**，现代标准为 **ISO 226:2003**，早期研究称为 Fletcher-Munson 曲线。

等响曲线表明：

- 在 1 kHz 附近，人耳最敏感；
- 在 100 Hz 以下和 8 kHz 以上，需要更高的声压级才能获得同样的响度；
- 低声压级下，这种差异更明显。

例如，在 80 phon 和 60 phon 两条等响曲线之间：

- 100 Hz 处声压级差可达 10–15 dB；
- 10 kHz 处声压级差可达 5–10 dB；
- 1 kHz 处差异较小，通常小于 5 dB。

因此，低音量补偿通常表现为：

- 低频 shelf 提升；
- 高频 shelf 轻微提升；
- 中频基本不变或略微调整。

除了等响曲线，厂商还会结合目标曲线，如：

- Harman Target Curve；
- Diffuse Field / Free Field Target；
- 自家声学实验室的“品牌调音曲线”。

这些目标曲线用于决定不同频段的基础频响，而响度补偿则是在此基础上随音量动态叠加。

---

## 3. 典型处理链路

一个典型的低音量频响补偿链路如下：

```
数字音频输入
    ↓
音量检测 / 响度估计
    ↓
目标补偿曲线计算
    ↓
多段 EQ / 动态 EQ
    ↓
低频心理声学增强（可选）
    ↓
DRC / Limiter
    ↓
输出
```

在手机中，该链路通常运行在：

- 音频 DSP / aDSP；
- 音频 Codec 内置 DSP；
- 系统音频框架中的后处理模块；
- 耳机内置 DSP，通过手机 App 配置参数。

---

## 4. 核心算法模块

### 4.1 音量/响度估计

补偿增益与“实际听音响度”相关，但手机只能直接获得系统音量，无法直接知道用户耳内的 dB SPL。

常见做法：

- **数字音量映射**：系统音量百分比映射到 dB 衰减，如 100% → 0 dB，50% → -20 dB；
- **耳机灵敏度校准**：通过耳机 ID、Type-C 耳机信息、蓝牙设备信息估算 dB SPL；
- **参考耳机假设**：实验室用“典型耳机”测量，建立音量百分比与估计 SPL 的查找表；
- **蓝牙 A2DP Absolute Volume**：手机与耳机同步音量，但仍需估计耳机灵敏度；
- **麦克风反馈**：部分高端耳机可通过内置麦克风估计实际声压级，但较少用于消费级低音量补偿。

实际产品中，多数手机采用“参考耳机模型”或“用户选择的耳机类型”进行近似估计。

### 4.2 补偿曲线计算

给定当前估计回放声压级 \(L_{cur}\) 和参考声压级 \(L_{ref}\)，补偿增益可表示为：

\[
G(f) = clamp(EQ_{target}(f, L_{ref}) - EQ_{target}(f, L_{cur}), 0, G_{max})
\]

其中 \(EQ_{target}\) 是目标响度曲线，\(G_{max}\) 是防止过量提升的最大增益。

实际实现中，不会全量补偿等响差异，通常：

- 低频提升上限约 6–12 dB；
- 高频提升上限约 2–6 dB；
- 补偿随音量平滑过渡，避免突变。

### 4.3 滤波器实现

低延迟场景通常使用 IIR biquad 滤波器，而非 FIR，因为：

- IIR 延迟低；
- 计算量小；
- 适合低音/高音 shelf 和 PEQ。

常用滤波器类型：

| 频段     | 滤波器类型 | 典型参数                                       |
| -------- | ---------- | ---------------------------------------------- |
| 低频     | Low Shelf  | f0: 100–150 Hz，增益 0–12 dB，斜率 6–12 dB/oct |
| 高频     | High Shelf | f0: 8–12 kHz，增益 0–6 dB，斜率 6–12 dB/oct    |
| 中频修正 | PEQ        | 根据目标曲线微调，Q: 0.5–2                     |
| 低频保护 | High Pass  | f0: 20–40 Hz，防止次声波失真                   |

动态 EQ 方面，厂商会将 shelf 增益与音量联动，并使用时间平滑：

```c
// 平滑过渡，防止音量变化时产生突兀
alpha = 1.0 - exp(-1.0 / (tau * fs));   // tau 约 100–500 ms
current_gain += alpha * (target_gain - current_gain);
```

### 4.4 动态范围控制与限幅

低音量补偿会提升整体电平，可能导致大信号削波。因此通常需要：

- **预衰减**：在 EQ 前先衰减 3–6 dB，为提升留 headroom；
- **Limiter**：在输出前限制峰值，threshold 约 -1 dBFS，ratio 20:1；
- **DRC**：夜间模式或“深夜 EQ”会使用多段压缩，提升弱信号、压低强信号。

### 4.5 低频心理声学增强

如果耳机或手机扬声器物理上无法重放强低频，厂商会使用心理声学低音增强，如：

- **Waves MaxxBass**：通过生成低频谐波，让人脑“脑补”低频；
- **Sony Clear Bass**：动态低音增强，可能包含谐波生成；
- **Bose Active EQ**：部分产品结合心理声学低音。

这类方法能在不增加低频振幅的情况下提升低音感知，特别适合小尺寸单元。

---

## 5. 各厂商实现概览

### 5.1 Apple

Apple 在 iOS / macOS 中提供多种音频后处理能力，与低音量补偿相关的主要有：

- **耳机调节（Headphone Accommodations）**  
  路径：设置 → 辅助功能 → 音频/视觉 → 耳机调节。  
  用户可以自定义“平衡音、音域、亮度”，并增强“软声音”，分为轻度、中度、强力。  
  实现上，系统会基于用户听力图或手动设置，生成个性化频响补偿和动态压缩，提升弱信号。这本质上是响度补偿的一种应用。

- **Apple Music 深夜 EQ**  
  这是 Apple Music 内置 EQ 预设之一，会压缩动态范围并提升安静部分，适合小音量聆听。它属于动态响度补偿，而不是简单静态 EQ。

- **AirPods 自适应 EQ**  
  AirPods Pro / Max 使用耳内麦克风实时测量频响，自动调整低频和中频。它主要补偿佩戴差异，但在低音量下也能帮助保持频响稳定。

Apple 的算法细节未公开，但整体路径是：系统音频框架 → 个性化音频处理 → 多段 EQ/DRC → 耳机输出。

### 5.2 Samsung / Harman

三星手机和耳机产品中常见的相关功能：

- **Adapt Sound**  
  用户进行听力测试，手机会生成个性化 EQ。该 EQ 可针对不同频率提升，尤其改善低音量下用户听不到的频段。

- **SoundAlive**  
  三星系统级音效，提供多种 EQ 预设和低音增强。部分版本包含“音量监视器”或“响度补偿”。

- **Galaxy Buds 系列**  
  Galaxy Wearable 应用提供 EQ 预设，部分型号支持“自适应 EQ”。三星与 AKG/Harman 合作，调音通常基于 Harman Target Curve。

Harman 作为三星子公司，其目标曲线和声学调音被广泛用于 JBL、AKG 产品。低音量补偿可能通过 App 端 EQ 和耳机 DSP 实现。

### 5.3 Sony

索尼在 Walkman、耳机和手机中提供丰富的音频增强功能：

- **Sony Headphones Connect App**  
  提供 5 段/10 段 EQ、Clear Bass、DSEE HX/Extreme、Dynamic Normalizer。

- **ClearAudio+**  
  自动分析音乐，调整频响和声场。在低音量下，它可能自动提升低频和高频，但官方未明确说明。

- **Dynamic Normalizer**  
  主要用于曲目间响度一致，不是严格意义上的响度补偿，但可减少音量差异。

- **Sound Optimizer / Loudness**  
  索尼家庭影院和 AV 接收器中常见“Sound Optimizer”，在低音量时保持低频和高频饱满。其耳机产品可能继承类似算法思路。

索尼的 DSEE Extreme 使用 AI 恢复高频细节，与响度补偿不同，但能提升小音量下的高频感知。

### 5.4 Bose

Bose 是少数明确宣传“音量优化 EQ”的厂商。

- **Active EQ / Volume-optimized EQ**  
  Bose QuietComfort Earbuds、Bose Soundbar 等产品宣传“Active EQ 可在任何音量下保持饱满音质”。  
  实现路径为：耳机内置 DSP 检测数字音量 → 调整低频和高频 shelf → 在低音量下提升低频和高频。

- **特点**  
  强调自动、实时、无需用户干预。Bose 拥有多项相关专利，涉及基于音量的动态 EQ 和扬声器保护。

### 5.5 Dolby / Audyssey / Dirac / Waves

这些是第三方音频算法供应商，其技术被多家手机、电视、AVR 厂商采用。

- **Dolby Volume**  
  基于感知响度模型，将音频分成多频带，对每个频带计算目标响度，并动态补偿。  
  它包含音量建模和音量调平两级，能在不同音量下保持一致的音色和动态。  
  手机中的 Dolby Audio 可能启用部分 Dolby Volume 能力。

- **Audyssey Dynamic EQ**  
  主要用于 Denon/Marantz AV 接收器。根据主音量设置，结合房间校正，动态调整频响和环绕声。  
  其核心是等响曲线补偿，移动端较少见，但原理被广泛借鉴。

- **Dirac HD Sound / Dirac Bass**  
  Dirac 与 OPPO、OnePlus、小米等手机厂商合作，提供频响和相位校正。  
  Dirac Bass 通过心理声学增强低音，适合小尺寸扬声器和低音量场景。

- **Waves MaxxBass / MaxxVolume**  
  MaxxBass 生成低频谐波，增强低音感知；MaxxVolume 提供响度补偿和动态控制。  
  常用于笔记本和部分手机。

### 5.6 手机 SoC 与 Codec 厂商

- **Qualcomm**  
  骁龙音频 DSP 支持厂商部署响度补偿算法。Snapdragon Sound 平台提供可编程滤波、DRC、EQ 硬件加速。

- **Cirrus Logic**  
  CS47L 系列 smart codec 内置 DSP，提供 SoundClear 音频增强库，包括 EQ、DRC、扬声器保护、响度补偿模块。

- **联发科、展讯等**  
  类似地，在音频 DSP 中提供厂商可定制的后处理框架。

这些底层厂商不直接定义最终音质，而是提供工具，由手机厂商或第三方算法公司实现具体补偿曲线。

---

## 6. 调整细节与参数示例

以下为典型低音量频响补偿参数，不代表任何具体厂商，但可作为工程参考。

### 6.1 补偿曲线示例

| 估计回放 SPL | 低音 Shelf 增益 @ 100 Hz | 高音 Shelf 增益 @ 10 kHz | 1 kHz 增益 |
| ------------ | ------------------------ | ------------------------ | ---------- |
| 80 dB SPL    | 0 dB                     | 0 dB                     | 0 dB       |
| 70 dB SPL    | +3 dB                    | +1.5 dB                  | 0 dB       |
| 60 dB SPL    | +6 dB                    | +3 dB                    | 0 dB       |
| 50 dB SPL    | +10 dB                   | +5 dB                    | 0 dB       |

说明：实际补偿量通常小于等响曲线理论值，以避免失真和听感不自然。

### 6.2 滤波器配置示例

```yaml
low_shelf:
  type: LowShelf
  freq: 120 Hz
  gain: +8 dB
  Q: 0.707

high_shelf:
  type: HighShelf
  freq: 10 kHz
  gain: +3 dB
  Q: 0.6

pre_gain:
  amount: -3 dB

limiter:
  threshold: -1 dBFS
  release: 100 ms
  ratio: 20:1
```

### 6.3 动态平滑参数

- 攻击时间：100–300 ms  
- 释放时间：500–1000 ms  
- 目的是避免用户快速调音量时出现明显“频响跳动”。

---

## 7. 工程实现注意事项

1. **低频保护**  
   低频提升过多会导致小扬声器失真，需加入高通滤波器，通常 20–40 Hz。

2. **高频控制**  
   高频提升过多会突出齿音和噪声，可配合 de-esser 或限制高频最大增益。

3. **延迟与功耗**  
   手机和真无线耳机对功耗敏感，优先使用 IIR biquad 和硬件 DSP，避免高延迟 FIR。

4. **个性化**  
   结合用户听力测试或年龄，可显著提升低音量补偿效果。Apple 和三星都提供了个性化听力测试。

5. **内容自适应**  
   某些内容本身低频多，补偿后可能过量。高级算法会分析内容频谱，动态调整补偿量。

6. **耳机类型差异**  
   入耳式、头戴式、开放式耳机灵敏度不同，理想的补偿需要耳机校准。部分厂商通过耳机 ID 识别或用户选择耳机型号实现。

---

## 8. 总结

低音量下的频响补偿是手机和耳机音频后处理中的重要环节，其核心是：

- 基于等响曲线，动态提升低频和少量高频；
- 使用 shelf / PEQ / 动态 EQ 实现；
- 配合 DRC 和 Limiter 防止削波；
- 可选心理声学低音增强；
- 结合个性化听力测试优化效果。

各厂商实现路径大同小异，差异主要在于目标曲线、补偿强度、个性化程度和动态平滑策略。公开信息中，Bose 的 Active EQ 和 Dolby Volume 是最明确的响度补偿技术；Apple、三星、索尼则更倾向于将响度补偿融入个性化音效和系统音频框架中。

---

## 9. 附录：示例伪代码

```python
import math

def estimate_spl(system_volume_percent, headphone_sensitivity_db_per_v=100.0):
    """
    粗略估算耳内 SPL。
    实际产品会使用更复杂的耳机模型和校准数据。
    """
    # 假设 100% 音量对应 0 dBFS，参考声压级约 100 dB SPL
    volume_db = 20 * math.log10(system_volume_percent / 100.0 + 1e-6)
    spl = headphone_sensitivity_db_per_v + volume_db
    return spl

def loudness_compensation_gain(freq_hz, current_spl, ref_spl=80.0, max_gain_db=12.0):
    """
    简化响度补偿增益计算。
    实际中需查等响曲线表，或使用多项式拟合。
    """
    # 简化：低频增益随 SPL 降低而增加
    if freq_hz < 200:
        low_freq_factor = 1.0 - (freq_hz / 200.0)  # 越低频补偿越多
        gain_db = (ref_spl - current_spl) * 0.5 * (0.2 + low_freq_factor)
    elif freq_hz > 8000:
        high_freq_factor = (freq_hz - 8000) / 8000.0
        gain_db = (ref_spl - current_spl) * 0.15 * (0.2 + high_freq_factor)
    else:
        gain_db = (ref_spl - current_spl) * 0.05  # 中频几乎不补偿

    return max(0.0, min(gain_db, max_gain_db))

def apply_shelf_filter(samples, freq, gain_db, fs):
    """
    应用低音/高音 shelf 滤波器。
    实际使用 biquad 系数，这里仅为示意。
    """
    # 省略 biquad 实现
    pass

# 主处理循环
def process_audio(input_samples, system_volume_percent):
    current_spl = estimate_spl(system_volume_percent)
    output = []
    for sample in input_samples:
        # 根据频率计算增益
        # 实际中需先分频段处理
        low_gain = loudness_compensation_gain(100, current_spl)
        high_gain = loudness_compensation_gain(10000, current_spl)
        # 平滑增益
        # ...
        # 应用滤波器
        # ...
        output.append(sample)
    return output
```

---

**报告完。**